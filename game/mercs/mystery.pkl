amends ".../engine/faction/merc.pkl"
import ".../engine/unit/army.pkl" as Army

slug = "mystery-mercs"
name = "???"
description = "It's probably sanctuary. I will forever lament the loss of the edolian and endari merc factions."
in_game = false

local faction_id = this.slug

talents {
  default { 
    tag { "merc" }
    column = 3
    faction = faction_id
  }
}

units {
  default { 
    faction = faction_id 
    tag { "merc" }
  }
}
