amends ".../engine/unit/army.pkl"

name = 
hotkey = 
description = 
tier = 
hexite_cost = 
build_time = 
supply = 
build_count = 
hp = 
armor_type = 
speed = 

attacks {
  new {
  }
}

upgrades {
  new {
    tier = "T1.5"
  }
  new {
    tier = "T1.5"
  }
  
  new {
    tier = "T2.5"
  }

  new {
    tier = "T2.5"
  }
}

notes {
}

/*


import registry from "../../../registry.js";

export const Harbinger = registry
  .grellUnit("grell/army/harbinger", "Harbinger")
  .hotkey("w")
  .t1()
  .producedBy("grell/building/incubator")
  .cost(100, 100)
  .buildTime(25)
  .supply(3)
  .ground()
  .hp(300)
  .heavyArmor()
  .speed(500)
  .primaryAttack("primary", "Primary Attack", (ability) =>
    ability.directDamage(21).range(800).reload(1.75).targets("ground", "air"),
  )
  .spell("eruption", "Eruption", (ability) =>
    ability
      .targets("location")
      .hotkey("e")
      .range(1400)
      .abesEnergy(10)
      .delay(1.5)
      .aoe(100, 100)
      .note("not sure if aoe radius is correct, might be slightly below 100"),
  );
*/
