module talent
extends "data.pkl"

typealias TalentLevel = Int(isBetween(1, 5))
typealias TalentColumn = Int(isBetween(1, 3))

fixed type = "talent"

level: TalentLevel
column: TalentColumn

/// normally would be a topbar ability if anything? just needs any valid data id 
unlocks: Listing<String>?
